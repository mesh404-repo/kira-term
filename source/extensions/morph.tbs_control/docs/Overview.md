# TBS Control

USD 로드(외부 URL / 로컬·resource 경로), 장비 prim 제어창, 가상 이벤트 애니메이션 테스트.

- **USD Load**: 경로 입력 후 Load로 스테이지 로드. 기본값은 `DEFAULT_USD_URL`.
- **TBS 제어창**: 로드된 USD 내 모든 prim을 드롭다운으로 표시. 각 영역에 객체 정보(USD), Position X/Y/Z, button_0(직선 순차), button_1(포물선).
