sim_sequences 템플릿 폴더

- 이 폴더의 JSON은 `config/event_animation_map.json` / `config/event_animation_rules.json`에서 참조됩니다.
- 시퀀스 편집기에서 저장한 JSON(list 루트)을 같은 파일명으로 덮어쓰면 시뮬레이션 이벤트 시 자동 실행됩니다.
- 비어있는 템플릿은 모두 `[]` 입니다.
